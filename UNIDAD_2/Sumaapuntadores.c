#include <stdio.h>
int main(){
    //Declare integer variables too, fno, sno, sum, and integer pointers pir, qtr
    int fno, sno, sum, *ptr, *qtr;

    printf("\n\n Pointer : Add two numbers : \n");
    printf("---------------------------------\n");
}